import SwiftUI

struct SupportSettingsView: View {
    var body: some View {
        List {
            // TODO: Add support settings
        }
        .navigationTitle("Support")
        .navigationBarTitleDisplayMode(.large)
    }
}

#Preview {
    SupportSettingsView()
        .preferredColorScheme(.dark)
}
