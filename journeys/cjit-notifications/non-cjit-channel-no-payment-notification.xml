<journey name="non-cjit channel opening shows no payment notification">
  <description>
    Opening a regular (non-CJIT) Lightning channel must NOT be reported as a received payment: no
    "Incoming Payment" notification, and never several duplicates. The correct notification for this
    path is the orderPaymentConfirmed copy, "Spending Balance Ready". When the app is in the
    foreground the only feedback is the in-app "Spending balance ready" toast.

    No extension log assertion here: with the app in the foreground the node holds the .lightning
    process lock, and NotificationService returns on that lock before it decrypts or logs anything.
    The observable evidence is the toast plus the absence of an "Incoming Payment" notification.

    Precondition: physical device with an onboarded regtest wallet connected to the LSP and a
    positive on-chain balance to fund the transfer. No CJIT entry is involved — this is a manual
    Transfer channel opening (orderPaymentConfirmed), not a CJIT receive.
  </description>
  <actions>
    <action>Keep the app in the foreground on the wallet home screen</action>
    <action>Open a regular spending channel via Transfer → Spending, funding some sats and confirming</action>
    <action>Mine confirmations if required, against regtest — note ./lsp is a bitkit-android helper and is not available in this repo</action>
    <action>Wait for the ChannelReady event (~5-10s)</action>
    <action>Verify an in-app "Spending balance ready" toast appears (id "SpendingBalanceReadyToast")</action>
    <action>Open Notification Center and inspect the Bitkit entries</action>
    <action>Verify NO "Incoming Payment" notification was posted for this channel opening</action>
    <action>Verify any notification posted for it uses the "Spending Balance Ready" copy and not the "Incoming Payment" copy</action>
    <action>Verify there are not multiple duplicate channel-ready notifications stacked in Notification Center</action>
  </actions>
</journey>
