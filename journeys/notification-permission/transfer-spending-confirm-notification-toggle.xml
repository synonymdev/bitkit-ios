<journey name="transfer spending confirm toggle requests notification permission">
  <description>
    Verifies that the "Set up in background" toggle on the Transfer → Spending confirm screen
    launches the iOS notification authorization alert, and that allowing it checks the toggle.

    Precondition: onboarded dev wallet with a POSITIVE on-chain Savings balance and the node
    connected to the LSP (so a real max can be quoted), notification authorization still
    notDetermined (reinstall first — the alert is one-shot), and "Set up in background" starting
    off. The spending max starts at 0 behind a spinner — wait for it to populate. Start on the
    wallet home screen. Enter via the Savings card, not the Spending card — the Spending screen
    only offers "Transfer From Savings" while the spending balance is zero.
  </description>
  <actions>
    <action>Tap the Savings balance card on the home screen (id "ActivitySavings")</action>
    <action>Tap "Transfer To Spending" (id "TransferToSpending")</action>
    <action>If a transfer intro screen appears, tap "Get Started"</action>
    <action>On the spending amount screen (id "SpendingAmount"), wait until the available/maximum amount (id "SpendingAmountAvailable") finishes loading and shows a positive value</action>
    <action>Enter an amount within the maximum using the number pad</action>
    <action>Tap Continue (id "SpendingAmountContinue") and wait for the spending confirm screen to appear</action>
    <action>Verify the "Set up in background" toggle (id "SpendingConfirmNotificationSwitch") is visible and off</action>
    <action>Tap the "Set up in background" toggle (id "SpendingConfirmNotificationSwitch")</action>
    <action>Verify the iOS notification authorization alert appears (text like "Bitkit" Would Like to Send You Notifications")</action>
    <action>Tap "Allow"</action>
    <action>Verify the "Set up in background" toggle is now on</action>
  </actions>
</journey>
