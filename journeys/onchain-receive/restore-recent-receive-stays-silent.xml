<journey name="recent receive stays silent after a seed restore">
  <description>
    Covers Android issue #1342, fixed on iOS in #588. A deposit confirmed within the last hour, restored
    from its seed, must not show the received sheet: its confirmation is inside the one-hour window,
    and a replayed or late-handled confirmation could reach the presenter after the first sync had
    lifted the restore hold. The first onchain sync after a restore records its chain tip, and
    confirmed-only receives at or below it stay silent. A deposit mined after the restore still shows
    the sheet.

    Precondition: a throwaway simulator (both halves uninstall the app; never run this on a device
    holding a wallet you need). If onboarding does not show after reinstalling, the keychain survived
    the uninstall: run `xcrun simctl keychain booted reset` and relaunch. The wallet is the public
    BIP39 test vector "abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon
    abandon about", so other developers may have funded it on the shared regtest chain; only the
    deposit made here is asserted on. Put the words on the simulator pasteboard with
    `printf '&lt;words&gt;' | xcrun simctl pbcopy booted` and paste them into "Word-0" rather than typing
    them. Run the whole journey within the hour, or the one-hour window alone keeps the sheet silent
    and the run proves nothing.
  </description>
  <actions>
    <action>Run: xcrun simctl uninstall booted to.bitkit, install and launch the build, accept the terms, tap "SkipIntro", "RestoreWallet" and "MultipleDevices-button"</action>
    <action>Paste the test vector into "Word-0", tap "RestoreButton", then "GetStartedButton", and dismiss any intro sheet until the home screen shows (id "TotalBalance-primary")</action>
    <action>Tap Receive (id "Receive"), tap the "Savings" receive tab (id "Tab-savings"), tap "Show Details" (id "ShowDetails"), read the address from id "ReceiveOnchainAddress", and swipe the Receive sheet down to the home screen</action>
    <action>Run in one command: ../bitkit-android/lsp POST /regtest/chain/deposit '{"address":"&lt;savings addr&gt;","amountSat":21342}' &amp;&amp; ../bitkit-android/lsp POST /regtest/chain/mine '{"count":1}'</action>
    <action>Verify within 30s the received sheet (id "ReceivedTransaction") shows 21 342 sats, then tap "ReceivedTransactionButton"</action>
    <action>Run: xcrun simctl uninstall booted to.bitkit, install and launch the build and restore the same test vector the same way, tap "GetStartedButton" and dismiss any intro sheet</action>
    <action>Wait 60s on the home screen</action>
    <action>Verify the received sheet (id "ReceivedTransaction") never appeared and the home screen shows "TotalBalance-primary"</action>
    <action>Run: grep "Skipping received sheet for confirmed-only tx" "$(ls -t "$(xcrun simctl get_app_container booted to.bitkit group.bitkit)"/logs/bitkit_*.log | head -1)"</action>
    <action>Verify the log skipped the confirmed-only receive for the deposit's txid</action>
    <action>Read a new address as above and run in one command: ../bitkit-android/lsp POST /regtest/chain/deposit '{"address":"&lt;savings addr&gt;","amountSat":21343}' &amp;&amp; ../bitkit-android/lsp POST /regtest/chain/mine '{"count":1}'</action>
    <action>Verify within 30s the received sheet (id "ReceivedTransaction") shows 21 343 sats</action>
  </actions>
</journey>
