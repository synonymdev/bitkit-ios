<journey name="Paykit Issuer Interoperability">
  <description>
    Verifies that the canonical accepted regtest issuer fixture reaches Bitkit and opens the payment confirmation flow. Requires the saved and linked server fixture plus the funded regtest wallet described in README.md. The canonical fixture uses "bitkit/server"; a Bitkit app acting as issuer uses its negotiated "bitkit/wallet" path instead. Rejected shapes are covered by the shared fixture unit tests because Bitkit intentionally does not present them.
  </description>
  <actions>
    <action>Launch the E2E Bitkit app with Paykit UI enabled and the fixture issuer saved as a contact and linked on receiver path "bitkit/server"</action>
    <action>Have the issuer publish a current regtest P2WPKH address under identifier "btc-regtest-p2wpkh" with JSON payload {"value":"&lt;current address&gt;"}</action>
    <action>Have the issuer send proposed one-time Payment Request "71300000-0000-4000-8000-000000000001" for amount "0.001", asset "btc", and accepted identifier "btc-regtest-p2wpkh"</action>
    <action>Verify the Payment Request confirmation screen (id "PaymentRequestConfirm") appears with 100,000 sats</action>
    <action>Dismiss the Payment Request confirmation screen without paying</action>
    <action>Tap the pending Payment Requests bell (id "PaymentRequestsBell")</action>
    <action>Verify the incoming Payment Requests sheet (id "PaymentRequestsSheet") appears</action>
    <action>Using the saved issuer public key, verify the one-time request row (id "PaymentRequestRow-71300000-0000-4000-8000-000000000001-&lt;fixture-issuer-public-key&gt;-bitkit/server-one-time") shows 100,000 sats</action>
    <action>Tap Pay (id "PaymentRequestPay-71300000-0000-4000-8000-000000000001")</action>
    <action>Verify the Payment Request confirmation screen (id "PaymentRequestConfirm") shows 100,000 sats</action>
    <action>Tap Show details (id "SendConfirmToggleDetails")</action>
    <action>Verify the recipient (id "ReviewContactRecipient") is the saved fixture issuer contact</action>
  </actions>
</journey>
