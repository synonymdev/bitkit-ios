<journey name="requested unresolvable payment request shows terminal feedback">
  <description>
    Verifies a user explicitly opening an incoming Payment Request receives localized terminal
    feedback after resolution retries exhaust, while the request remains available for another
    attempt.

    Precondition: onboarded dev wallet with Paykit UI enabled, a profile, and one linked saved
    contact. Seed exactly one proposed incoming Payment Request from that contact with a known
    payment-request id and a supported accepted endpoint identifier, while the controlled Paykit
    peer returns no matching endpoint for at least 35 seconds. Start on Payment Requests (id
    "PaymentRequestsScreen").
  </description>
  <actions>
    <action>Verify the incoming request row (id "PaymentRequestRow-&lt;payment-request-id&gt;") is visible</action>
    <action>Tap Pay (id "PaymentRequestPay-&lt;payment-request-id&gt;")</action>
    <action>Wait up to 35 seconds for the terminal error toast (id "PaymentRequestUnavailableToast")</action>
    <action>Verify the toast title is "Payment Request" and its description is "The payment request is no longer available."</action>
    <action>Verify Payment Requests (id "PaymentRequestsScreen") remains visible</action>
    <action>Verify the incoming request row (id "PaymentRequestRow-&lt;payment-request-id&gt;") remains visible for a later retry</action>
  </actions>
</journey>
