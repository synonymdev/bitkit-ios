<journey name="Subscriptions Payments Tab">
  <description>
    The Payments tab inside Subscriptions: the pending badge on the tab, an incoming request with its
    Dismiss and Pay actions, an outgoing request showing pending in place of the fiat amount, and the
    payment request detail screen. Requires one incoming and one outgoing Paykit Payment Request.

    iOS names the detail screen "PaymentRequestDetailScreen", singular, where Android uses
    "PaymentRequestDetailsScreen"; see the identifier table in README.md. It has no hardware back
    button either, so the last step goes through the navigation bar's back arrow (id "NavigationBack").
    Recurring payments of one subscription all carry the same paymentRequestId, so iOS appends the
    billing period to keep the rows distinct where Android stops at the id. The shared
    "PaymentRequestRow-&lt;paymentRequestId&gt;" prefix still matches on both platforms.
  </description>
  <actions>
    <action>Launch the E2E Bitkit app with Paykit UI enabled, one incoming Payment Request pending, and one outgoing Payment Request awaiting the recipient</action>
    <action>Open the drawer menu and tap Subscriptions</action>
    <action>Verify the Payments tab (id "Tab-payments") shows a badge with the pending incoming count, rendered immediately after the label with an 8pt gap, the label still centred in the tab, and its indicator level with the other tabs</action>
    <action>Tap the Payments tab</action>
    <action>Verify the payment requests list (id "PaymentRequestsScreen") appears under the PAYMENT REQUESTS header</action>
    <action>Verify the incoming request card shows Dismiss and Pay actions</action>
    <action>Verify the outgoing request row shows "pending" where the fiat amount sits</action>
    <action>Verify every row carries the bitcoin symbol before its amount</action>
    <action>Tap the incoming request row (id "PaymentRequestRow-&lt;paymentRequestId&gt;-&lt;billingPeriodStart&gt;", or "-one-time" for a one-off request)</action>
    <action>Verify the details screen (id "PaymentRequestDetailScreen") shows DATE, TIME, CONTACT, TAGS and NOTE sections</action>
    <action>Verify the contact card is sized to its content rather than the full width, and that a divider follows the contact and tags sections</action>
    <action>Verify the note card is drawn with the torn paper top edge</action>
    <action>Tap the back arrow (id "NavigationBack") and verify the list is shown again, still on the Payments tab</action>
    <action>Verify Request Payment (id "PaymentRequestRequestPayment") is visible at the bottom and that list rows scroll underneath it</action>
  </actions>
</journey>
