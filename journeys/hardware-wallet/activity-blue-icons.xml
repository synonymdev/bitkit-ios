<journey name="Hardware Wallet Activity Blue Icons">
  <description>
    Verifies hardware wallet on-chain activity in the unified home and All Activity lists,
    with wallet-scoped blue icons and tab filters. Requires a paired Bridge emulator with at
    least two confirmed receives with distinct amounts and one Transfer To Spending activity.
  </description>
  <actions>
    <action>Launch the Bitkit app and go to the wallet home screen</action>
    <action>Verify the recent activity list contains at least one blue hardware activity row, then tap "Show All" (id "ActivityShowAll") beneath the activity list</action>
    <action>Verify All Activity contains two blue received rows with distinct amounts and one blue Transfer row with subtitle "From Savings"</action>
    <action>Verify the blue Transfer row appears exactly once and no default-wallet row repeats its amount or transaction</action>
    <action>Tap the blue Transfer row with subtitle "From Savings"</action>
    <action>Verify the detail screen shows a blue hardware icon, title "From Savings", and "TO SPENDING", then navigate back</action>
    <action>Tap the "Sent" tab (id "Tab-sent") and verify the blue received rows and blue Transfer row are not listed</action>
    <action>Tap the "Received" tab (id "Tab-received") and verify both distinct blue received rows are listed while the blue Transfer row is absent</action>
    <action>Tap the "Other" tab (id "Tab-other") and verify the blue Transfer row is listed while both blue received rows are absent</action>
    <action>Tap the "All" tab (id "Tab-all") and verify both blue received rows and the blue Transfer row are listed again</action>
  </actions>
</journey>
