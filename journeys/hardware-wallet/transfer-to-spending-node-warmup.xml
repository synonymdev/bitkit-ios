<journey name="Hardware Wallet Transfer To Spending Node Warmup">
  <description>
    Verifies that starting a hardware-wallet transfer while the Lightning node is still
    warming up does not fail or strand the CTA in loading. The flow should show the normal
    loading/progress UI, continue once the node reaches the needed state, and land on the
    sign screen. Requires a paired and funded hardware wallet.
  </description>
  <actions>
    <action>Force-quit Bitkit: xcodebuildmcp simulator stop --bundle-id to.bitkit</action>
    <action>Relaunch it: xcodebuildmcp simulator launch-app --bundle-id to.bitkit</action>
    <action>As soon as the wallet home screen is visible, tap the hardware wallet tile (id "ActivityHardware") beneath the SAVINGS and SPENDING tiles</action>
    <action>Tap the "Transfer To Spending" button (id "HardwareTransferToSpending")</action>
    <action>Verify the transfer amount screen or loading/progress UI appears, and no reconnect, node-not-ready, or generic failure toast is shown while the node warms up</action>
    <action>Tap the "25%" quick button (id "HardwareTransferAmountQuarter") if the amount screen is shown</action>
    <action>Tap "Continue" (id "HardwareTransferAmountContinue") if the amount screen is shown, then wait for order creation and node warm-up to finish</action>
    <action>Verify the sign screen opens (id "HardwareTransferSign"), titled "SIGN WITH YOUR DEVICE", and the Continue/Open Trezor Connect CTA is no longer loading</action>
    <action>Navigate back without signing so this journey only covers node warm-up behavior</action>
  </actions>
</journey>
