<journey name="Hardware Wallet Settings">
  <description>
    Verifies the Hardware Wallets settings surface: the Hardware Wallets row with the paired-device
    count, the Hardware Wallets screen listing the paired device (name, balance, connection
    indicator), the Add Hardware Wallet button opening the connect intro sheet, and the
    per-row delete confirm dialog. The final Remove forgets the device, so this re-pairs the
    emulator at the end so other journeys can run afterwards. Requires a paired Bridge
    emulator (run connect-home-tile.xml first).
  </description>
  <actions>
    <action>Launch the Bitkit app, open the menu, and navigate to Settings (id "DrawerSettings")</action>
    <action>Ensure the "General" tab (id "Tab-general") is selected and scroll down to the "Hardware Wallets" row</action>
    <action>Verify the "Hardware Wallets" row (id "HardwareWalletsSettings") shows a numeric value of at least 1</action>
    <action>Tap the "Hardware Wallets" row</action>
    <action>Verify the Hardware Wallets screen opens (id "HardwareWalletsScreen") with the top bar titled "Hardware Wallets"</action>
    <action>Verify the paired device is listed with its name, a bitcoin balance prefixed with the ₿ symbol, and a green connection indicator on the left</action>
    <action>Tap the paired device name on the Hardware Wallets screen</action>
    <action>Verify the "Rename Hardware Wallet" sheet opens (id "RenameHardwareWalletInput") with the current name prefilled</action>
    <action>Clear the Name field, type "Renamed Trezor", tap "Save" (id "RenameHardwareWalletSave"), and verify the sheet closes</action>
    <action>Verify the Hardware Wallets screen now lists the paired device as "Renamed Trezor"</action>
    <action>Tap the "Add Hardware Wallet" button near the bottom (id "AddHardwareWallet")</action>
    <action>Verify a sheet opens titled "Hardware Wallet" (id "HardwareWalletIntroScreen") showing hardware device illustrations, then tap Cancel (id "HardwareWalletIntroCancel") and verify the sheet closes back to the Hardware Wallets screen</action>
    <action>Tap the trash (delete) icon on the device row</action>
    <action>Verify a confirm dialog appears (id "RemoveHwWalletDialog") explaining that your funds are safe and your coins won't be deleted</action>
    <action>Tap "Cancel" and verify the device is still listed on the Hardware Wallets screen</action>
    <action>Tap the trash (delete) icon again, then tap "Remove" in the confirm dialog</action>
    <action>Verify the device is removed: the list no longer shows it and an empty state is displayed</action>
    <action>Navigate back to Settings and verify the "Hardware Wallets" row value decreased (or the row shows 0)</action>
    <action>Open the menu, navigate to Settings, then Dev Settings, then tap the "Trezor" row, tap "Scan" (id "TrezorScanButton"), and tap the discovered device to re-pair it</action>
    <action>Verify the device connects within 15 seconds</action>
  </actions>
</journey>
