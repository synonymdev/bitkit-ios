<journey name="Hardware Wallet Connect and Home Tile">
  <description>
    Pairs the Bridge Trezor emulator through the dev Trezor screen, then verifies the
    home-screen hardware wallet tile: name, connection indicator, balance, and the
    hardware wallet detail screen. Requires the bitkit-docker Trezor User Env running
    and a Bridge-enabled build (see README.md).
  </description>
  <actions>
    <action>Launch the Bitkit app and dismiss any onboarding or authentication prompts if a wallet already exists</action>
    <action>Open the menu, navigate to Settings (id "DrawerSettings"), then Dev Settings</action>
    <action>Tap the "Trezor" row</action>
    <action>Tap the "Scan" button (id "TrezorScanButton")</action>
    <action>Verify a device named "Bitkit Test Trezor" or "Trezor" (id "TrezorDevice-bridge") appears in the device list within 10 seconds</action>
    <action>Tap the listed device to connect</action>
    <action>Verify a toast or status shows the device as connected within 15 seconds, with no PIN or pairing prompt</action>
    <action>Navigate back to the wallet home screen</action>
    <action>Verify a hardware wallet tile (id "ActivityHardware") is shown beneath the SAVINGS and SPENDING tiles, labelled with the device name in caps (e.g. "BITKIT TEST TREZOR")</action>
    <action>Verify the hardware tile shows a green connection indicator icon next to its name and a blue bitcoin icon with a sats amount</action>
    <action>Tap the hardware wallet tile</action>
    <action>Verify the hardware wallet detail screen opens (id "HardwareWalletScreen"), showing the device name with a blue bitcoin icon in the top bar and a balance header</action>
    <action>Navigate back to the wallet home screen</action>
  </actions>
</journey>
