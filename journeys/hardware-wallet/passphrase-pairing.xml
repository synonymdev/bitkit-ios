<journey name="Hardware Wallet Passphrase Pairing">
  <description>
    Adds a passphrase (hidden) wallet of an already paired Trezor: from the Paired step the
    Passphrase button opens Enter Passphrase, and entering one watches the wallet it unlocks as a
    separate identity with its own funds label. Verifies the home screen then shows two hardware
    tiles and counts both in the headline balance. Requires the emulator started with passphrase
    protection enabled (TREZOR_PASSPHRASE_PROTECTION=true ./scripts/trezor-emulator start) and a
    paired Bridge emulator (run connect-home-tile.xml first).
  </description>
  <actions>
    <action>Launch the Bitkit app, open the menu, navigate to Settings, ensure the "General" tab (id "Tab-general") is selected, and tap the "Hardware Wallets" row (id "HardwareWalletsSettings")</action>
    <action>Note the paired-device count shown, then tap the "Add Hardware Wallet" button (id "AddHardwareWallet")</action>
    <action>Tap "Continue" (id "HardwareWalletIntroContinue") and wait for the Found Device step (id "HardwareWalletFoundScreen"), then tap "Connect" (id "HardwareWalletFoundConnect")</action>
    <action>Verify the sheet reaches the "Device Connected" step (id "HardwareWalletPairedScreen"), showing both a "Passphrase" button (id "HardwareWalletPairedPassphrase") and a "Finish" button (id "HardwareWalletPairedFinish")</action>
    <action>Tap "Passphrase" (id "HardwareWalletPairedPassphrase")</action>
    <action>Verify the Passphrase step opens (id "HardwareWalletPassphraseScreen") headed "Enter passphrase", showing the shield illustration, and that "Continue" (id "HardwareWalletPassphraseContinue") is disabled while the input is empty</action>
    <action>Type "bitkit-hidden" into the passphrase field (id "HardwareWalletPassphraseInput"), then tap "Continue" (id "HardwareWalletPassphraseContinue")</action>
    <action>
      Confirm on the emulated device for every account the passphrase session reads: the device
      prompts once per address type and the call blocks until it is acknowledged. Either tap the
      emulator UI or run: ../bitkit-docker/scripts/trezor-emulator send-json '{"type":"emulator-press-yes","id":1}'
    </action>
    <action>Verify the sheet advances to the passphrase paired step (id "HardwareWalletPassphrasePairedScreen") headed "Passphrase funds found", showing a balance and an editable "Label Funds" field prefilled with the device name</action>
    <action>Clear the "Label Funds" field (id "HardwareWalletLabelInput") and type "Hidden Trezor"</action>
    <action>Tap "Finish" (id "HardwareWalletPairedFinish") and verify the sheet closes</action>
    <action>Navigate to the wallet home screen and verify two hardware wallet tiles are shown beneath the SAVINGS and SPENDING tiles, one of them labelled "Hidden Trezor"</action>
    <action>Verify the headline total balance is at least the sum of both hardware tile balances</action>
    <action>Tap the "Hidden Trezor" tile and verify its hardware wallet detail screen opens (id "HardwareWalletScreen") titled "Hidden Trezor"</action>
    <action>Resolve the app group container against the simulator under test and scan it, treating only "no match" as clean: UDID=&lt;the simulator this journey is running on&gt;; GROUP=$(xcrun simctl get_app_container "$UDID" to.bitkit groups | awk '{print $2}'); [ -d "$GROUP" ] || { echo "LEAK_CHECK_INVALID: container not found"; exit 1; }; grep -rl "bitkit-hidden" "$GROUP"; s=$?; case $s in 1) echo NO_PASSPHRASE_LEAK;; 0) echo "LEAK FOUND";; *) echo "LEAK_CHECK_INVALID: grep exit $s";; esac</action>
    <action>Verify the previous command printed NO_PASSPHRASE_LEAK. LEAK_CHECK_INVALID means the check did not scan anything and the journey cannot conclude either way — treat it as a failed step, not a pass</action>
    <action>Grep the newest app log for errors: grep -iE "error|exception" "$(ls -t "$GROUP"/logs/*.log | head -1)" | tail -20 — if no log file matches, say so rather than treating the empty result as clean</action>
    <action>Verify the previous command reported no Trezor connect, session or watcher errors while the hidden wallet was paired</action>
  </actions>
</journey>
