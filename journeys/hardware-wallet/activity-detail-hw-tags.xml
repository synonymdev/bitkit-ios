<journey name="Hardware Wallet Activity Tags And Inputs Outputs">
  <description>
    Verifies wallet-scoped Core tags and Electrum transaction details for a hardware activity.
    Requires a paired Bridge emulator with at least two confirmed hardware receives and the
    tag "hwtest" absent from the selected activity before the run.
  </description>
  <actions>
    <action>Launch the Bitkit app and go to the wallet home screen</action>
    <action>Tap "Show All" (id "ActivityShowAll") beneath the activity list</action>
    <action>Tap a blue hardware received row that is not the Transfer row</action>
    <action>Verify the activity detail screen shows a blue icon and the selected received amount</action>
    <action>Tap the "Tag" button (id "ActivityTag") — it is labelled "Tag", not "Add Tag"</action>
    <action>Type "hwtest" into the tag field (id "TagInput") and tap "Add" (id "ActivityTagsSubmit")</action>
    <action>Verify a tag chip labelled "hwtest" is shown in the detail screen's chip list (id "ActivityTags")</action>
    <action>Navigate back, reopen the same blue hardware row, and verify the "hwtest" tag is still shown</action>
    <action>Tap "Explore" (id "ActivityTxDetails")</action>
    <action>Verify the Activity Explorer screen shows an Inputs section and an Outputs section, each with at least one entry</action>
    <action>Navigate back to All Activity, open the tag filter (id "TagsPrompt"), and select "hwtest"</action>
    <action>Verify the tagged blue hardware activity remains listed and the other untagged hardware rows are absent</action>
  </actions>
</journey>
