<journey name="Hardware Wallet Detail Overview">
  <description>
    Opens the hardware wallet detail screen from the home tile and verifies its overview:
    the top bar, balance header, the Transfer To Spending entry when funds are present,
    the grouped activity list, and the Remove device confirm dialog. The final
    Remove step forgets the device, so run this last (re-run connect-home-tile.xml to pair
    again). Requires a paired Bridge emulator (run connect-home-tile.xml first).
  </description>
  <actions>
    <action>Launch the Bitkit app and go to the wallet home screen</action>
    <action>Tap the hardware wallet tile (id "ActivityHardware") beneath the SAVINGS and SPENDING tiles</action>
    <action>Verify the hardware wallet detail screen opens (id "HardwareWalletScreen"), showing the device name with a blue bitcoin icon in the top bar and a balance header</action>
    <action>If a "Transfer To Spending" button is shown (id "HardwareTransferToSpending"), tap it, verify the transfer amount screen opens (id "HardwareTransferAmount") titled "TRANSFER TO SPENDING", then navigate back to the hardware wallet detail screen; otherwise skip this step</action>
    <action>If the activity list shows transactions, verify their circular icons are blue, then tap the first one, verify an activity detail screen opens, and navigate back</action>
    <action>Tap the "Remove" button labelled with the device name near the bottom of the screen (id "RemoveHardwareWallet")</action>
    <action>Verify a confirm dialog appears (id "RemoveHwWalletDialog") explaining that your funds are safe and your coins won't be deleted</action>
    <action>Tap "Cancel" and verify the hardware wallet detail screen is still shown</action>
    <action>Tap the "Remove" button labelled with the device name again, then tap "Remove" in the confirm dialog</action>
    <action>Verify the app returns to the wallet home screen and no hardware wallet tile is shown</action>
  </actions>
</journey>
