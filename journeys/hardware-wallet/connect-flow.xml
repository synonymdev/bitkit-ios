<journey name="Hardware Wallet Connect Flow From Settings">
  <description>
    Verifies the Connect Hardware flow entered from Hardware Wallets settings: forgets the
    paired device so it can be rediscovered, opens Settings then Hardware Wallets, taps Add,
    and runs Searching, Found and Paired, editing the Label Funds field before finishing.
    Confirms the paired device is listed under its custom label, which re-pairs the emulator
    so later journeys can run. Requires a paired Bridge emulator (run connect-home-tile.xml
    first).
  </description>
  <actions>
    <action>Launch the Bitkit app, open the menu, navigate to Settings, then Dev Settings, then tap the "Trezor" row</action>
    <action>Tap the forget-device (trash) icon next to the known device (id "TrezorForgetDevice-bridge"), confirming any dialog, so the device becomes discoverable again</action>
    <action>Navigate back to Settings, ensure the "General" tab (id "Tab-general") is selected, and tap the "Hardware Wallets" row (id "HardwareWalletsSettings")</action>
    <action>Verify the Hardware Wallets screen (id "HardwareWalletsScreen") shows no paired devices</action>
    <action>Tap the "Add Hardware Wallet" button (id "AddHardwareWallet")</action>
    <action>Verify a sheet opens titled "Hardware Wallet" (id "HardwareWalletIntroScreen"); tap the "Continue" button (id "HardwareWalletIntroContinue")</action>
    <action>Verify the sheet advances to the "Connect Device" step (id "HardwareWalletSearchingScreen") headed "Searching for devices"</action>
    <action>If iOS shows a Bluetooth or Local Network permission prompt, allow it and continue waiting on the Searching step</action>
    <action>Verify the sheet advances to the "Found Device" step (id "HardwareWalletFoundScreen") headed "Found Trezor" within 15 seconds, then tap "Connect" (id "HardwareWalletFoundConnect")</action>
    <action>Verify the sheet advances to the "Device Connected" step (id "HardwareWalletPairedScreen") headed "Paired Trezor", showing a balance and an editable "Label Funds" field, with no PIN or pairing prompt</action>
    <action>Clear the "Label Funds" field (id "HardwareWalletLabelInput") and type "My Trezor"</action>
    <action>Tap the "Finish" button (id "HardwareWalletPairedFinish") and verify the sheet closes back to the Hardware Wallets screen</action>
    <action>Verify the Hardware Wallets screen now lists one paired device named "My Trezor" with a green connection indicator</action>
  </actions>
</journey>
