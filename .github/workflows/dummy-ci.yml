name: Dummy CI

on:
  push:
    branches: [master]
  pull_request:
  workflow_dispatch:

jobs:
  dummy:
    if: github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Dummy CI completed"
